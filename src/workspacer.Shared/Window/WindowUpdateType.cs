namespace workspacer
{
    public enum WindowUpdateType
    {
        Show,
        Hide,
        MinimizeStart,
        MinimizeEnd,
        Foreground,
        MoveStart,
        MoveEnd,
        Move,
    }
}
